# noBadWords
A real-time lightweight software to suppress/censor swear words from audio (videos, discord calls, etc.).

# (dev) How to Run
1. Activate Client Virtual Environment: Run ".venv/Scripts/Activate.ps1"
2. Build Container: docker compose up app
3. Run "python ./client/audio_capture.py"